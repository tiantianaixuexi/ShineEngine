I will implement a decoupled OpenGL rendering module within `src/render/demo` and integrate it into `EditorView.cpp`.

### 1. New Module: `OpenGLDemoRenderer`
I will create a self-contained renderer class to handle the raw OpenGL operations.
- **Files**:
  - `src/render/demo/OpenGLDemoRenderer.h`
  - `src/render/demo/OpenGLDemoRenderer.cpp`
- **Features**:
  - **Init()**: Sets up shaders, VAO/VBO/EBO for a Cube and a Grid. Initializes OpenGL state (Depth Test, Clear Color).
  - **Render()**: Handles the render loop.
    - Binds the Viewport's Framebuffer (via `IRenderBackend`).
    - Clears the screen.
    - Updates rotation logic using `shine::math` (Internal math library, replacing the requested `glm` as it is not present in `src/third`).
    - Draws the Grid (Lines) and Cube (Triangles) using raw OpenGL commands.
    - Performs `glGetError()` checks.
  - **Shutdown()**: Cleans up GL resources.

### 2. Integration: `EditorView`
I will modify `EditorView` to use the new renderer.
- **`src/editor/views/EditorView.h`**:
  - Add `std::unique_ptr<OpenGLDemoRenderer> m_DemoRenderer;`.
- **`src/editor/views/EditorView.cpp`**:
  - **`Init()`**: Initialize `m_DemoRenderer`.
  - **`Render()`**:
    - Calculate `deltaTime`.
    - Call `m_DemoRenderer->Render(...)` passing the backend and viewport handle.
    - This replaces or augments the existing `renderer->renderView` call to ensure the raw OpenGL content is drawn to the FBO.

### 3. Build System
- The new files in `src/render` will be automatically picked up by the existing CMake configuration (which globs `src/**/*.cpp`), so no `Module/*.json` modification is strictly necessary for `MainEngine` compilation.

### 4. Verification
- I will verify the code compiles.
- I will provide a README with instructions and details as requested.
